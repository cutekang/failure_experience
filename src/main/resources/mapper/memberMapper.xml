<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.tms_failure.mapper.MemberMapper">
    <insert id="insert" parameterType="MemberVO">
        INSERT INTO TBL_MEMBER_TASK
        VALUES(SEQ_MEMBER_TASK.NEXTVAL, #{memberEmail}, #{memberPassword}, #{memberName}, #{memberAddress}, #{memberPhone})
    </insert>

    <select id="selectIdByEmailAndPassword" parameterType="MemberVO" resultType="Long">
        SELECT ID
        FROM TBL_MEMBER_TASK
        WHERE MEMBER_EMAIL = #{memberEmail} AND MEMBER_PASSWORD = #{memberPassword}
    </select>

    <select id="selectById" parameterType="Long" resultType="MemberVO">
        SELECT ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME, MEMBER_ADDRESS, MEMBER_PHONE
        FROM TBL_MEMBER_TASK
        WHERE ID = #{id}
    </select>

    <update id="update" parameterType="MemberVO">
        UPDATE TBL_MEMBER_TASK
        SET MEMBER_EMAIL = #{memberEmail}, MEMBER_PASSWORD = #{memberPassword}, MEMBER_ADDRESS = #{memberAddress}, MEMBER_PHONE = #{memberPhone}
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE
        FROM TBL_MEMBER_TASK
        WHERE ID = #{id}
    </delete>
</mapper>